<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.FastScroll" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FastScroll" parent="Base.Theme.FastScroll" />

    <style name="FastScrollerTextAppearance" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@color/background</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="ContactsPopupTextViewStyle" parent="TextAppearance.AppCompat">
        <item name="android:textSize">@dimen/contacts_popup_text_size</item>
        <item name="android:textColor">@android:color/white</item>

    </style>
</resources>